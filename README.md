#INSTALL PREMIUM SCRIPT V2 BY JAYVPN

```
apt update && apt upgrade -y && apt install -y wget screen && wget -q https://raw.githubusercontent.com/jayvpn/scriptvps/main/setup.sh && chmod +x setup.sh && screen -S setup ./setup.sh
```
